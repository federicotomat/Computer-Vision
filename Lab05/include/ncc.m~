%% Normalized cross correltion

function box = ncc(imageGray, selectedArea)
n_target = size(imageGray,1)  * size(imageGray,2);
n_template =  size(selectedArea,1)  * size(selectedArea,2);
mi_f = 






end